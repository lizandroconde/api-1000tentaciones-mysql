// Imports

import {GraphQLObjectType} from 'graphql'

// App Imports

// Mutation
const query = new GraphQLObjectType({
  name: 'query',
  description: '...',

  fields: {
   
  
    
  }
})

export default query
